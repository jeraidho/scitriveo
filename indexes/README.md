# Index artifacts storage

--- 

- Here is the directory that contains the artifacts of core indexes: `bm25`, `word2vec`, `fasttext`
- Directory `indexes` is necessary for **SciTriveo** running
- Each index has its own directory with artifacts and metadata
