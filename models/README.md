# Pretrained models storage

--- 

- Here is the directory that contains pretrained vectors and models for FastText or Word2Vec (or any other that is needed in SciTriveo)
- Directory `models` is not necessary for **SciTriveo** running, you can initialise your own models from other directories
